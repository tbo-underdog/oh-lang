# build artifacts — rebuilt from source, never committed
compiler/oh
translator/ohtranslate
*.o
a.out
# demo binaries built in the repo root
/api
/life
# intermediate IR/asm anywhere EXCEPT the committed freestanding runtime + start stubs
*.ll
*.s
!tooling/*.s
!tooling/*.ll
# compiled benchmark binaries
*_bench
